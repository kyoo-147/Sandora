# Operations index

Load only the document needed by the current decision:

- `HERDR.md` — create, address, prompt, monitor, and read peer agent sessions.
- `MODEL_ROUTING.md` — select Codex, Command Code, or AGY and an appropriate
  model tier.

These documents are operating guidance. Live CLI help, installed model lists,
and observed benchmark results override stale examples.

